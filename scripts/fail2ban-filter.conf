# Fail2ban filter — Defumados AC login failures
# Copie para /etc/fail2ban/filter.d/defumadosac-login.conf
# Depois: sudo fail2ban-client reload

[Definition]
failregex = .*Tentativa de login inv\xe1lida para usu.*
ignoreregex =
